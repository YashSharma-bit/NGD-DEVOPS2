# ============================================================
# India Regional Development Analytics System
# Python Dependencies
# ============================================================

# ── Core Data ─────────────────────────────────────────────────
pandas==2.2.2
numpy==1.26.4
openpyxl==3.1.4
xlrd==2.0.1
pyarrow==16.0.0          # Parquet I/O for large datasets

# ── Geospatial ────────────────────────────────────────────────
geopandas==0.14.4
shapely==2.0.4
pyproj==3.6.1
fiona==1.9.6
rasterio==1.3.10          # Nighttime lights / raster processing
rasterstats==0.19.0       # Zonal statistics from rasters

# ── Database ──────────────────────────────────────────────────
sqlalchemy==2.0.30
psycopg2-binary==2.9.9
geoalchemy2==0.15.1       # PostGIS spatial types
neo4j==5.20.0             # Neo4j Python driver
alembic==1.13.1           # Schema migrations

# ── ETL / Networking ──────────────────────────────────────────
requests==2.32.2
httpx==0.27.0
aiohttp==3.9.5
tenacity==8.3.0           # Retry logic
tqdm==4.66.4              # Progress bars
rapidfuzz==3.9.3          # Fuzzy district name matching
unidecode==1.3.8          # Unicode → ASCII normalization

# ── Analytics / ML ────────────────────────────────────────────
scikit-learn==1.5.0
scipy==1.13.1

# ── Visualization ─────────────────────────────────────────────
plotly==5.22.0
matplotlib==3.9.0
mapclassify==2.7.0        # Choropleth classification schemes
contextily==1.6.0         # Basemap tiles

# ── API ───────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.1
pydantic-settings==2.3.0
slowapi==0.1.9            # Rate limiting
redis==5.0.4
aioredis==2.0.1

# ── Configuration & Utilities ─────────────────────────────────
python-dotenv==1.0.1
pyyaml==6.0.1
loguru==0.7.2
click==8.1.7              # CLI scripts
rich==13.7.1              # Terminal output

# ── Jupyter (notebooks) ───────────────────────────────────────
jupyter==1.0.0
ipykernel==6.29.4
folium==0.16.0
ipywidgets==8.1.3

# ── Testing ───────────────────────────────────────────────────
pytest==8.2.0
pytest-asyncio==0.23.7
pytest-cov==5.0.0
httpx==0.27.0             # AsyncClient for FastAPI testing
